import { describe, it } from '@jest/globals';

describe.skip('App e2e', () => {
  it('pending database-backed e2e setup', () => {
    // Covered by integration tests once DATABASE_URL test environment is provisioned.
  });
});
